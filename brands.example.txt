# Copy to brands.txt and edit. Whole-brand watches — alert on ANY new product
# or restock from the brand. One brand per line, as Shopbop spells it
# (case-insensitive, accents OK).
#
# No department = BOTH men's and women's, so co-ed/unisex items are never
# missed (and a co-ed item alerts only once). Add "| MENS" or "| WOMENS" to
# restrict a brand to one department.
#
# Only exact-designer matches are kept, so resellers (e.g. "Shopbop Archive")
# and diffusion lines (e.g. "MM6 Maison Margiela") are excluded automatically.
AGOLDE
Reformation
Our Legacy | MENS
